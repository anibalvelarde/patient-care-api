# renormalize eol & lfs
*.sh text eol=lf

# WP-17B-2: generated by tools/generate-permissions.sh (LF, no CR) — pin so a Windows
# checkout (CRLF) never false-trips the codegen --check diff or breaks the C# string literals.
src/Web/Authorization/Permissions.g.cs text eol=lf
